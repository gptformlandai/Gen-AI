def probe_video(video_path: str) -> dict[str, str]:
    return {
        "video_path": video_path,
        "status": "not_implemented",
        "next_step": "Use FFmpeg or MoviePy here in a later lesson.",
    }
